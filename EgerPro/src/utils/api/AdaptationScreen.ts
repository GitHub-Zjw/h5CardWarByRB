module api
{
	export class AdaptationScreen
	{
		public constructor()
		{
		}

		// /**
		//  * 强制填充屏幕
		//  */
		// public SetNoScale(com: eui.Component): void
		// {
		// 	let aox = document.documentElement.clientWidth;
		// 	let aoy = document.documentElement.clientHeight;
		// 	com.height = aoy;
		// 	com.width = aox;
		// }

		// /**
		//  * 强制左右居中
		//  */
		// public SetCenter(com: eui.Component): void
		// {
		// 	let aox = document.documentElement.clientWidth;
		// 	let aoy = document.documentElement.clientHeight;
		// 	let h = com.height;
		// 	let changeSy = aoy / h;
		// 	let w = com.width * changeSy;
		// 	// com.x = (aox - w) / 2;
		// 	GameConfig.curStage().x = (aox - w) / 2;
		// 	GameConfig.curWidth();
		// 	GameConfig.curHeight()
		// 	// com.y = (aoy - h) / 2;
		// }
	}
}